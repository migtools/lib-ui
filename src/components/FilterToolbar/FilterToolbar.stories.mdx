import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { useFilterState } from '../../hooks/useFilterState';

import { FilterToolbar } from './FilterToolbar';
import { FilterableTable } from './FilterToolbar.stories.tsx';
import GithubLink from '../../../.storybook/helpers/GithubLink';

<Meta title="Components/FilterToolbar" component={FilterToolbar} />

# FilterToolbar

A custom component for filtering table values.

<!-- Not sure if there is a way to pull in this signature from the source automatically, that'd be better -->

## Examples

### Filterable table

<Canvas>
  <Story story={FilterableTable} />
</Canvas>

<GithubLink path="src/hooks/useSelectionState/useFilterState.ts" />
